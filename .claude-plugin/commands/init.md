---
description: Initialize the Mnemosyne palace (master key + default sealed vault)
---

Run `mnemosyne init` and report the result. If a passphrase-derived master key is wanted, remind the user to export MNEMOSYNE_PASSPHRASE first.
