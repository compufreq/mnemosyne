---
description: Mine files or conversation transcripts into the palace
---

Ask which directory to mine if not given as $ARGUMENTS. For documents run `mnemosyne mine <dir> --wing <project>`; for Claude Code sessions run `mnemosyne mine <dir> --mode convos --wing <project>`. Report drawers filed.
